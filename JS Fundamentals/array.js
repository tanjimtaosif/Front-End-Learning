var array = [1, 2, 3, 4, 5, 6];
// console.log(array.length); // to see the legnth of the array
// array.push(8);
// array.pop(); last element will be eleminated
// array.unshift(0); // It add in the [0] index
array.shift(); // It will remove the first number
console.log(array);