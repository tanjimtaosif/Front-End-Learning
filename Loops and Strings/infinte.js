// this is an infinite loop because here i is greater then 0 and we are also trying to increment the iteration when condition is satisfied so it will print infinity times 

for (let i = 1; i > 0; i++) {
    console.log("i=", i);

}